# Simple 2D N-body Simulator in Python

This code is a simple N-body solver in a 2D space.
DIP files are used to set default values, let the user to modify them, and calculate secondary, derived, settings

You can run this code using the following:

``` bash
python3 ./main.py
```

