# Electromagnetic Force Simulator in C++

This is a simple example, how to integrate DIP into a C++/CMake building process.
You an try to run it with the following command:

```bash
python3 simulation.py --init --build --run --plot
```

Additionally, one can export a DIP parameter documentation into a PDF file:

```bash
python3 simulation.py --docs
```
